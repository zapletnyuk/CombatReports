<Window x:Class="CombatReports.DataBaseContentDisplaying"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Icon="Assets/Military-Camouflage.ico"
        Title="Файли бази даних" 
        Height="500" 
        Width="490"
        MinHeight="500"
        MinWidth="490">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <DataGrid AutoGenerateColumns="False" x:Name="ordersGrid">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding FileName}" Header="Назва файлу" Width="300"/>
                <DataGridTextColumn Binding="{Binding FileData}" Header="Вміст файлу" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
